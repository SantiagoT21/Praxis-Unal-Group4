pipeline {
  agent any

  stages {
      stage('Docker compose Up') {
          steps {
              sh 'docker-compose down --volumes'
              sh 'docker-compose up -V'
          }
      }

  }
}